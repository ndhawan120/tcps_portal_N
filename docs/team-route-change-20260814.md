# Team route canonicalization — 2026-08-14

Canonical manager/admin team reporting route: `/team`.

Legacy `/manager` is retained only as a compatibility redirect to `/team`.

Internal navigation and links should use `/team`.
